# crudAngular
steps to execute the app
1- npm i - command used to install all the node modules
2- ng s - command to run the app in localhost:4200
3- In another terminal give the following path for json serve i.e src\assets\data
4- json-server --watch db.json use this command to run json server